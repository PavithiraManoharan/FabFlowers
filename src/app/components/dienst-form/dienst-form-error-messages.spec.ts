import { DienstFormErrorMessages } from './dienst-form-error-messages';

describe('DienstFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new DienstFormErrorMessages()).toBeTruthy();
  });
});
